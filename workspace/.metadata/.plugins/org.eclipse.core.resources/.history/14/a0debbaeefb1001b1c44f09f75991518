package ex05;

import java.io.IOException;
import java.util.Optional;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

/**
 * Servlet implementation class Ex05
 */
@WebServlet("/Ex05") // URL Mapping 이다.
public class Ex05 extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
    /**
     * @see HttpServlet#HttpServlet()
     */
    public Ex05() {
        super();
        // TODO Auto-generated constructor stub
    }

	/**
	 * @see HttpServlet#doGet(HttpServletRequest request, HttpServletResponse response)
	 */
	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		// TODO Auto-generated method stub
		
		// 파라미터 처리 
		
		// 1. 인코딩(파라미터 처리하는 과정에서 항상 처음으로 해줘야하는 작업)
		request.setCharacterEncoding("UTF-8");
		
		// 2. 파라미터 저장
		String name = request.getParameter("name");
		Optional<String> opt1 = Optional.of(name); // String name 을 Optional로 한번 감싸 null값을 처리할 수 있도록해준다.(Optional은 null값도 처리할 수 있는 클래스이다.)
		System.out.println(opt1.get()); // Optional로 감싼 데이터를 get() 메소드로 꺼내사용할 수 있다.
		
	}

	/**
	 * @see HttpServlet#doPost(HttpServletRequest request, HttpServletResponse response)
	 */
	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		// TODO Auto-generated method stub
		doGet(request, response);
	}

}
